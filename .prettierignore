# The architecture spec is locked. Prettier reflows its tables and breaks the
# rows whose cells contain pipe characters inside code spans.
docs/
node_modules/
dist/
coverage/
package-lock.json
coverage-baseline.json
playwright-report/
test-results/
tmp-data/
