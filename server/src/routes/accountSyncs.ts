import type { FastifyPluginAsync } from "fastify";

/** /api/account-syncs CRUD and manual run. */
export const accountSyncRoutes: FastifyPluginAsync = async () => {
  // TODO: register the routes for this resource.
};
