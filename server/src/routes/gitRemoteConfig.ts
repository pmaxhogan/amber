import type { FastifyPluginAsync } from "fastify";

/** /api/git-remote enable, disable, and rotate. */
export const gitRemoteConfigRoutes: FastifyPluginAsync = async () => {
  // TODO: register the routes for this resource.
};
