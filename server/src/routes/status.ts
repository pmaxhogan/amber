import type { FastifyPluginAsync } from "fastify";

/** /api/status totals, queue depth, and breaker state. */
export const statusRoutes: FastifyPluginAsync = async () => {
  // TODO: register the routes for this resource.
};
