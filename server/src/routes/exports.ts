import type { FastifyPluginAsync } from "fastify";

/** /api/repos/:id/export source and gitdir archives, tree, and blob. */
export const exportRoutes: FastifyPluginAsync = async () => {
  // TODO: register the routes for this resource.
};
