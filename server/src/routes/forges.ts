import type { FastifyPluginAsync } from "fastify";

/** /api/forges CRUD, kind detection on create. */
export const forgeRoutes: FastifyPluginAsync = async () => {
  // TODO: register the routes for this resource.
};
