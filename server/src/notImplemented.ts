/**
 * Marker for scaffolded functions a later pass fills in. Typed as `never` so a
 * skeleton body still satisfies whatever return type its signature declares.
 */
export function notImplemented(what: string): never {
  throw new Error(`${what} is not implemented yet`);
}
